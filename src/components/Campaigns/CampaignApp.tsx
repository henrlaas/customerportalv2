
import React from 'react';
import './animations.css';

// This is just a stub file to ensure our animations.css gets imported
// You don't need to use this directly, just import the CSS in your main.tsx or App.tsx
export const CampaignApp: React.FC = () => null;
